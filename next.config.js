/** @type {import('next').NextConfig} */
const nextConfig = {
    output: "export",
    assetPrefix: './animarum-web',

    images: {
        unoptimized: true,
    },

}

module.exports = nextConfig
